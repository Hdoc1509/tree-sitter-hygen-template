{
  "name": "tree-sitter-hygen-template",
  "version": "0.0.0",
  "license": "MIT",
  "author": "Héctor Ochoa <hector.ochoa.dev@gmail.com>",
  "description": "Hygen template grammar for tree-sitter",
  "main": "bindings/node",
  "types": "bindings/node",
  "packageManager": "pnpm@9.6.0",
  "files": [
    "grammar.js",
    "binding.gyp",
    "prebuilds/**",
    "bindings/node/*",
    "queries/*",
    "src/**",
    "*.wasm"
  ],
  "scripts": {
    "dev": "pnpm run _test:watchable --watch",
    "dev:one": "pnpm run _test:watchable --watch -- --filter",
    "generate": "wireit",
    "install": "node-gyp-build",
    "prestart": "tree-sitter build --wasm",
    "start": "tree-sitter playground",
    "test": "node --test bindings/node/*_test.js",
    "test:all": "pnpm run generate && tree-sitter test",
    "test:one": "pnpm run generate && tree-sitter test --filter",
    "_test:watchable": "wireit"
  },
  "repository": "github:Hdoc1509/tree-sitter-hygen-template",
  "keywords": [
    "hygen",
    "hygen-template",
    "template",
    "tree-sitter",
    "parser"
  ],
  "dependencies": {
    "node-addon-api": "8.0.0",
    "node-gyp-build": "4.8.1",
    "tree-sitter-embedded-template": "0.23.0"
  },
  "peerDependencies": {
    "tree-sitter": "0.21.1"
  },
  "peerDependenciesMeta": {
    "tree_sitter": {
      "optional": true
    }
  },
  "devDependencies": {
    "@changesets/cli": "2.27.8",
    "prebuildify": "6.0.1",
    "prettier": "3.3.3",
    "tree-sitter": "0.21.1",
    "tree-sitter-cli": "0.23.0",
    "wireit": "0.14.9"
  },
  "tree-sitter": [
    {
      "scope": "source.hygen_template",
      "file-types": [
        "hygen"
      ],
      "injection-regex": "^hygen_template$",
      "highlights": "queries/highlights.scm"
    }
  ],
  "wireit": {
    "generate": {
      "command": "tree-sitter generate"
    },
    "_test:watchable": {
      "command": "clear && echo 'Running parser tests...\n' && tree-sitter test",
      "dependencies": [
        "generate"
      ],
      "files": [
        "grammar.js",
        "test/corpus/*.txt"
      ]
    }
  }
}
